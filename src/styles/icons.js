import {css} from '@erickmerchant/css'

export const iconsClasses = css`
  .iconDefs {
    display: none;
  }

  .background {
    fill: var(--bg);
    stroke: var(--fg);
  }

  .foreground {
    fill: var(--fg);
    stroke: var(--bg);
  }
`
