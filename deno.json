{
  "tasks": {
    "watch": "export PORT=3000; export RUST_LOG=app=debug; export NO_CACHE=true; cargo watch -i theme -x'r --bin setup' -s'cargo run --no-default-features' & coolstyleserver --watch .",
    "lint": "cargo clippy",
    "update": "cargo update && cargo outdated -w -R",
    "docker:build": "docker build . -t my-blog",
    "docker:run": "docker run -p 3000:8080 my-blog"
  }
}
