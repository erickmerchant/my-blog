{
  "name": "erickmerchant.com-source",
  "private": true,
  "devDependencies": {
    "@erickmerchant/assets": "^4.0.0",
    "@erickmerchant/copy-files": "^1.2.0",
    "@erickmerchant/html": "^5.2.3",
    "@erickmerchant/ift": "^2.1.4",
    "@erickmerchant/optimize": "^3.0.2",
    "@erickmerchant/serve-files": "^2.0.1",
    "chalk": "^1.0.0",
    "geomicons-open": "^2.0.0",
    "lodash.groupby": "^4.6.0",
    "moment-timezone": "^0.5.11",
    "slug": "^0.9.1",
    "standard": "^10.0.0"
  },
  "dependencies": {},
  "scripts": {
    "test": "standard",
    "build": "copy-files base/* build && assets build && html build && optimize build build/bundle.css",
    "watch": "copy-files base/* build -w & assets build -w --no-min & html build -w --dev --no-min & serve-files build"
  }
}
