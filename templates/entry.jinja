{% extends "base.jinja" %}

{% block title %}
	{{site.title}} {% if tagged_entry.title %}— {{tagged_entry.title}}{% else %}Untitled{% endif %}
{% endblock %}

{% block main %}
	<header class="heading">
		<h1>{{tagged_entry.title}}</h1>
		{{sub_heading(tagged_entry)}}
	</header>

	{% if tagged_entry.content %}
		<div class="content">{{tagged_entry.content}}</div>
	{% endif %}
{% endblock %}
