{% extends "base.jinja" %}

{% block title %}
	{{site.title}} {% if entry.title %}— {{entry.title}}{% else %}Untitled{% endif %}
{% endblock %}

{% block main %}
	<header class="heading">
		<h1>{{entry.title}}</h1>
		{{sub_heading(entry)}}
	</header>

	{% if entry.content %}
		<div class="content">{{entry.content}}</div>
	{% endif %}
{% endblock %}
