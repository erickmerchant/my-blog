{% extends "_layout.jinja" %}

{% block main %}
  <header>
    <h1>{{page.title}}</h1>
    <p class="published">
      <time>{{page.date | date("%B %e, %Y")}}</time>
    </p>
  </header>

  {{page.content|safe}}

  {# <page-icon name="arrowLeft"></page-icon> #}
  {# <page-icon name="arrowRight"></page-icon> #}
{% endblock %}
