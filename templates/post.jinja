{% extends "_layout.jinja" %}

{% block title %}{{page.title}}{% endblock %}

{% block main %}
  <header>
    <h1>{{page.title}}</h1>
    <p class="published">
      <time>{{page.date | date("%B %e, %Y")}}</time>
    </p>
  </header>

  {{page.content|safe}}
{% endblock %}
