{% extends "layouts/_layout.jinja" %}

{%- macro pagination_link(page, position, icon) -%}
    <li>
      {% if page%}
        <a class="paginationLink paginationLink--{{position}}" href="/{{page.category}}/{{page.slug}}.html">
          <span class="paginationPageTitle">{{page.title}}</span> <svg-icon class="paginationIcon" name="{{icon}}"></svg-icon>
        </a>
      {% else %}
        <span class="paginationPlaceholder"></span>
      {% endif %}
    </li>
{%- endmacro -%}

{% block main %}
  <header>
    <h1>{{page.title}}</h1>
    <p class="published">
      <time>{{page.date | date("%B %e, %Y")}}</time>
    </p>
  </header>

  {{page.content | safe}}

  <ul class="pagination">
    {{pagination_link(pagination.previous, "previous", "arrowLeft")}}

    {{pagination_link(pagination.next, "next", "arrowRight")}}
  </ul>
{% endblock %}
