{% extends "layouts/base.jinja" %}

{% block main %}
  <header>
    <h1>{{page.title}}</h1>
    <p class="published">
      <time>{{page.date | format_date("%B %e, %Y")}}</time>
    </p>
  </header>

  {{page.content | safe}}
{% endblock %}
