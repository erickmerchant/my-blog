{% extends "layouts/base.jinja" %}

{% block main %}
<header>
	<h1>{{page.title}}</h1>
	<p class="published">
		{% if page.date %}
		<time>{{page.date | format_date("%B %e, %Y")}}</time>
		{% endif %}
	</p>
</header>

{{page.content}}
{% endblock %}
